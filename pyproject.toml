[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "natural-event-tracker"
version = "0.0.1"

dependencies = [
    "requests",
]

[tool.setuptools.packages.find]
where = ["backend"] 